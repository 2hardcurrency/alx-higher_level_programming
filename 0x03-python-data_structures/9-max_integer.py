#!/usr/bin/python3


def max_integer(my_list=[]):
    
    if len(my_list) == 0:
        return

    for i in range(1, my_list[i]:
        new_list = my_list.sort()
        max_value = new_list[-1]

        return max_value

