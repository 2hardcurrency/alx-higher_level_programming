#!/usr/bin/python3


def max_integer(my_list=[]):
    
    if len(my_list) == 0:
        return

    max_value = my_list[0]

    for i in range(1, len(my_list):
        if max_vlaue < my_list:
            max_value = my_list

            continue

            return max_value
