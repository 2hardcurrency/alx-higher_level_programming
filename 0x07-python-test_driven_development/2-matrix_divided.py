#!/usr/bin/python3
"""
Contains a function that divides a matrix all through
"""


def matrix_divided(matrix, div):
    """
    Definition of function that divides a matrix all through
    """
    if type(matrix) is not a list of list and
    if type(matrix) is not int and
    if type(matrix) is not float:
        raise TypeError("matrix must be a matrix (list of lists) of integers/floats")

